============
Installation
============

At the command line::

    $ easy_install ticket_admin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ticket_admin
    $ pip install ticket_admin